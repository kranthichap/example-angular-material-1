import { BinkDirective } from './bink.directive';

describe('BinkDirective', () => {
  it('should create an instance', () => {
    const directive = new BinkDirective();
    expect(directive).toBeTruthy();
  });
});
